<!DOCTYPE html>
<html>
<head>
    <title>React App Demo</title>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <!-- Don't use this in production: -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>
<body>
    <div id="root"></div>
    <script type="text/babel">
    const names = ["mike", "susan", "john", "rob"];

    const Greeting = (props) => {
        if (props.name) {
          return <h1>Hello!, {props.name}</h1>;
        }
          return <h1>Hello!</h1>;
        };
    const Item = ({ id, name }) => {
        return <li><a href={`/details/${id}`}>{name}</a></li>;
    };

    const List = ({ items }) => {
        return (
     <ul className="item-list">
         {items.map(item => <Item key={item.id} id={item.id} name={item.name} />)}
                </ul>
        );
     };

    const divStyle = {
        color: 'gray',
        backgroundImage: 'url(background_image.png)'
    };

    const WelcomeFunctional = (props) => {
        return <h1>Hello, {props.name}</h1>;
     };

    class WelcomeClass extends React.Component {
        render() {
        return <h1>Hello, {this.props.name}</h1>;
        }
    }

    

    const Students = (props) => {
    const names = ["mike", "smith", "john", "rob"];
    const listItems = names.map((item) => <li key={item}>{item}</li>);
        return <ul>{listItems}</ul>
    };

     const App = (props) => {
        return (
         <div>
            <h1 id="title">React Demo</h1>
                    <Greeting name={props.name} />
                    {names.length > 0 && <Students students={names} />}
                    <h2>Items List</h2>
                    <List items={items} />
                    <HelloWorldComponent />
        </div> 
        );
 };

    const container = document.getElementById('root');
    const root = ReactDOM.createRoot(container);
     root.render(<App name='IT' />);
    </script>
  </body>
</html>